Read This First. The purpose of this document is to allow easy copy paste functionality for the user when interacting with an external server.
Anything below this text should be commands only. No comments or section headers. It should only be commands will be replaced each time new commands are shared with the user.
The user will then copy and paste the commands on the server and share the output.

Separate commands to run on local computer and commands to run on a server or another computer.

Please overwrite everything below this line. Don't overwrite this line or above.

=== COMMANDS TO RUN ON LOCAL COMPUTER ===

cd ~/Downloads/Projects/nz-budget-calculator
git status
git add .
git commit -m "Add GitHub Actions deployment and fix backend health check

- Add /health endpoint for Docker healthcheck
- Fix duplicate sub-accounts bug in budget store
- Add automated deployment workflow
- Update .gitignore for security
- Add cleanup script for duplicate sub-accounts"
git push origin main
echo ""
echo "✅ Pushed to GitHub! Now go to:"
echo "https://github.com/h-gilbert/nz-budget-calc/actions"
echo ""
echo "Watch the deployment run in real-time!"

=== COMMANDS TO RUN ON SERVER (After deployment completes) ===

docker ps | grep budget
docker logs --tail 30 budget-backend
curl http://localhost:3200/health
docker inspect budget-backend --format='Health: {{.State.Health.Status}}'
